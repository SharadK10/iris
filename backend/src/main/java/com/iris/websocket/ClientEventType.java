package com.iris.websocket;

public enum ClientEventType {
    JOIN_ECHO,
    LEAVE_ECHO,
    PLAY,
    PAUSE,
    SEEK,
    ADD_TO_GARDEN,
    REMOVE_FROM_GARDEN,
    NEXT_BLOOM,
    TRANSFER_CONDUCTOR,
    HEARTBEAT
}
